<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- For the remote audio sources only. They are off until the user turns them on. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- SubRead Anki takes a card from the pop-up. Android hides other apps without this list. -->
    <queries>
        <package android:name="space.subread.anki" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar"
        android:usesCleartextTraffic="true">

        <!-- The settings: the dictionaries, the audio. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            The pop-up. Three ways in: the text selection menu of Android ("SubRead Dictionary"
            in the menu), the share sheet, and a plain intent from another app such as SubRead
            Overlay. It has no task of its own, so it opens over the app that sent the text.
        -->
        <activity
            android:name=".LookupActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/lookup_label"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/Popup">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="space.subread.dictionary.LOOKUP" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--
            Gives the terms of a text and the audio of a term to another app: SubRead Anki. The
            dictionaries are public data, so the provider has no permission. See LookupProvider.
        -->
        <provider
            android:name=".LookupProvider"
            android:authorities="${applicationId}.lookup"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <!-- SubRead Anki reads the audio of a card through this provider. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files" />
        </provider>
    </application>
</manifest>
